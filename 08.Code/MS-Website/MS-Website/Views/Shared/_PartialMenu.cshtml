@{
    var roleSession = (string)Session["Role"];
    string Admin = "Admin";
    string Staff = "Staff";
    string Customer = "Customer";
    string MaidMediator = "MaidMediator";
}

@if (Session["AccId"] != null)
{
    <div class="rows">
        <div class="hMenu">
            <div class="hFatherMenu">
                <ul class="hmeTabList">
                    @if (roleSession.Equals(Admin))
                    {
                        <li>
                            <a href="@Url.Action("BanAccount", "Admin")" class="hefaContent @(ViewBag.BanAcc != null ? "iscurent" : "")">Khóa tài khoản</a>
                        </li>
                        <li>
                            <a href="@Url.Action("AddStaff", "Admin")" class="hefaContent @(ViewBag.AddStaff != null ? "iscurent" : "")">Thêm nhân viên</a>
                        </li>
                        <li>
                            <a href="@Url.Action("ManageComment", "Admin")" class="hefaContent @(ViewBag.ManageComment != null ? "iscurent" : "")">Xóa bình luận</a>
                        </li>
                        <li>
                            <a href="@Url.Action("AdminConfig", "Admin")" class="hefaContent @(ViewBag.Kmean != null ? "iscurent" : "")">Kmean</a>
                        </li>
                    }
                    @if (roleSession.Equals(Customer))
                    {
                        <li>
                            <a href="@Url.Action("GetCustomer", "Customer", new { custId = (int)Session["AccId"] })" class="hefaContent @(ViewBag.Info != null ? "iscurent" : "")">Thông tin</a>
                        </li>
                    }
                    else if (roleSession.Equals("MaidMediator") || roleSession.Equals("Staff"))
                    {
                        <li>
                            <a href="@Url.Action("GetMaidManager", "MaidManager", new { accId = (int)Session["AccId"], role = roleSession })" class="hefaContent @(ViewBag.Info != null ? "iscurent" : "")">Thông tin</a>
                        </li>
                    }
                    @if (roleSession.Equals(MaidMediator) || roleSession.Equals(Staff))
                    {
                        <li>
                            <a href="@Url.Action("LoadAddMaid", "MaidManager")" class="hefaContent @(ViewBag.AddMaid != null ? "iscurent" : "")">Thêm người giúp việc</a>
                        </li>
                        <li>
                            <a href="@Url.Action("PostRequest", "MaidManager")" class="hefaContent @(ViewBag.AddJob != null ? "iscurent" : "")">Đăng yêu cầu công việc</a>
                        </li>
                    }
                    @if (roleSession.Equals(Customer))
                    {
                        <li>
                            <a href="@Url.Action("PostRecruitment", "Customer")" class="hefaContent @(ViewBag.AddRecruit != null ? "iscurent" : "")">Đăng yêu cầu tuyển việc</a>
                        </li>
                    }
                    @if (roleSession.Equals(Staff))
                    {
                        <li>
                            <a href="@Url.Action("ManageJobRequest", "MaidManager")" class="hefaContent @(ViewBag.StaffManage != null ? "iscurent" : "")">Quản lý</a>
                        </li>
                    }
                    @if (true)
                    {
                        <li>
                            <a href="@Url.Action("Statistic", "Statistic")" class="hefaContent @(ViewBag.Statistic != null ? "iscurent" : "")">Thống kê</a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </div>
}
